import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    // allow remote avatars from avatar.iran.liara.run
    domains: ["avatar.iran.liara.run"],
  },
};

export default nextConfig;
